releases:
  latest:
    candidate:
      product: okd
      stream: okd
      version: '4.4'
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e-aws
  cron: 12 8 9,21 * *
  steps:
    cluster_profile: aws
    workflow: openshift-e2e-aws
zz_generated_metadata:
  branch: master
  org: openshift
  repo: release
  variant: okd-4.4
